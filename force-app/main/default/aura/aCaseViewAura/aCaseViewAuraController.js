({
    myAction : function(component, event, helper) {

        // var pageRef = component.get('v.pageReference');
        // console.log('pageRef.state', pageRef.state);

        // let recordId = pageRef.state.c__aCaseId;
        // component.set('v.recordId', recordId);
    }
})